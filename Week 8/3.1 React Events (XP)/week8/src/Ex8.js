import React from 'react'

export default function GetChar(props) {
    const {input} = props;
    var chCode = props;
    alert ("The Unicode character code is: " + {props});
    return (
    <input size="40" onkeypress="GetChar (props);"/>
    )
}
